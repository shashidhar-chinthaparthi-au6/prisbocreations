/** Break out of `StoreShell` main padding so content can use the full viewport width. */
export const STOREFRONT_FULL_BLEED =
  "w-[100vw] max-w-[100vw] shrink-0 ml-[calc(50%_-_50vw)] mr-[calc(50%_-_50vw)]";

/** Horizontal padding aligned with the full-width home layout. */
export const STOREFRONT_GUTTER = "px-4 sm:px-6 lg:px-10";
